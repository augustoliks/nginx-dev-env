# nginx-dev-env

Enviroment Docker + NGINX + Docker-Compose, with hot-reload NGINX configure directory.

```shell
├── default.conf
├── docker-compose.yaml
├── hot-reload.sh
├── README.md
└── start.sh
```
